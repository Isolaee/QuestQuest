[package]
name = "units"
version = "0.1.0"
edition = "2021"

[dependencies]
graphics = { path = "../Graphics" }
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1.0", features = ["v4", "serde"] }

[dev-dependencies]
criterion = "0.5"